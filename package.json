{
  "name": "zaptools",
  "version": "1.0.0",
  "description": "ZapTools - Free online tools",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@cloudconvert/client": "^2.1.0",
    "node-fetch": "^2.6.7",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
